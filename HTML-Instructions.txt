# Replace the text in index.html with the following text from PRIMARY.html.
# sudo vi /var/www/html/index.html

# Replace the text in index.html with the following text from PRIMARY.html.
# sudo vi /var/www/html/index.html

# Replace the text in index.html with the following text from STANDBY.html.
# sudo vi /var/www/html/index.html

# After you have updated the index.html on there respected instance.
# run the following command.
# sudo systemctl restart httpd
